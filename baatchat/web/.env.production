# API on workers.dev (non-.app) so corporate filters that block the .app TLD (e.g. Telenor
# SafeZone) reach it fine. Frontend is served at chat.oblinor.no.
VITE_API_BASE=https://oblinor-borrower-chat.bk-39a.workers.dev
VITE_USE_MOCKS=false
